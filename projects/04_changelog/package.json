{
  "name": "changelog",
  "version": "0.1.0",
  "description": "File change tracking — immutable log of mutable resource changes",
  "type": "module",
  "bin": {
    "change": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
